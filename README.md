# Group-5-Project

Repo for CFG group assignment

# After cloning navigate to backend or cd backend and run

npm install express mysql2 cors dotenv

# Install MUI library by running,
npm install @mui/material @emotion/react @emotion/styled




## Notes when contributing this project (to be deleted before submission)

# When working in backend:

**Server**

- The server.js file starts the server
- Create a route for your endpoint in the routes folder, then link this within the server.js file. This will keep our API endpoints neat and contained
